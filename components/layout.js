const Layout = ({ children }) => {

    return (
        <div className="font-poppins">
            <main>
                {children}
            </main>
        </div>
    )
};

export default Layout;